# CTNSS - Cursor Rules

## Общие правила проекта

Это библиотека для работы с семантическими сетями, параметризацией и форсингом.
Код должен быть чистым, типизированным и следовать принципам иммутабельности.

## Технологии и версии

- Python 3.9+
- Без внешних зависимостей
- Использование стандартной библиотеки Python

## Стиль кодирования

### Импорты
- Всегда используй `from __future__ import annotations` в начале файла
- Группируй импорты: стандартная библиотека → внешние библиотеки → локальные импорты
- Используй абсолютные импорты для модулей внутри ctnss

### Типизация
- Используй аннотации типов для всех функций и методов
- Используй современный синтаксис типов: `list[T]`, `dict[K, V]`, `str | None` вместо `Optional[str]`
- Для frozen dataclass используй `FrozenSet` вместо `set`
- Для иммутабельных маппингов используй `Mapping` вместо `dict`

### Классы и структуры данных
- Используй `@dataclass(frozen=True)` для иммутабельных структур данных
- Используй `field(default_factory=...)` для коллекций по умолчанию
- Реализуй `__hash__` для классов, которые могут быть ключами
- Для создания модифицированных копий используй методы `with_*`

### Логика и вычисления
- Используй `TriBool` для трехзначной логики (TRUE/FALSE/UNKNOWN)
- Не приводи `TriBool` к `bool` напрямую
- Используй явные проверки: `result == TriBool.TRUE`

### Именование
- Классы: `PascalCase`
- Функции и методы: `snake_case`
- Константы: `UPPER_SNAKE_CASE`
- Приватные атрибуты: `_leading_underscore`
- ID концептов: `lowercase_snake_case`

## Архитектура модулей

### core/
Базовые структуры семантической сети:
- `Concept` - неизменяемые концепты с id, label, tags, meta
- `Predicate` - предикаты с арностью
- `Statement` - факты в сети
- `SemanticNetwork` - контейнер для концептов, предикатов и фактов

### param/
Параметризация и контексты:
- `Domain` - домены значений (EnumDomain, PredicateDomain)
- `Variable` - переменные с доменами
- `Context` - частичное присваивание значений переменным

### logic/
Логический слой:
- `Formula` - абстрактные формулы
- `FactAtom`, `Not`, `And`, `Or`, `Implies` - конкретные формулы
- `Term` - термы (ConstTerm, VarTerm)
- `Evaluator` - вычисление истинности формул

### forcing/
Форсинг-движок:
- `ForcingEngine` - основной движок для проверки и расширения контекстов
- `Conditions` - условия и ограничения
- `ForcingResult` - результат с explanation
- `Strategy` - стратегии поиска (пока не реализовано)

### io/
Сериализация и десериализация:
- JSON-совместимые форматы
- Читаемые представления для отладки

## Правила разработки

### При добавлении нового функционала:
1. Сохраняй иммутабельность структур данных
2. Добавляй типизацию для всех публичных API
3. Используй паттерн `with_*` для модификаций
4. Документируй сложную логику в docstrings
5. Пиши примеры использования в `examples/`

### При изменении существующего кода:
1. Не нарушай обратную совместимость
2. Сохраняй существующий стиль кодирования
3. Обновляй связанные примеры в `examples/`
4. Проверяй корректность типов

### Тестирование:
- Тесты в директории `tests/`
- Используй простые assert для проверок
- Тестируй граничные случаи (пустые сети, неопределенные значения)
- Проверяй все три состояния TriBool

### Примеры:
- Помещай примеры в `src/ctnss/examples/`
- Делай примеры самодостаточными
- Комментируй ключевые моменты для обучения
- Показывай типичные сценарии использования

## Стиль документации

### Общие принципы

Документация должна быть **формальной, точной и информативной**. Используй академический/технический стиль.

### Формальность текста

**Разрешено:**
- Нейтральные утверждения: "Библиотека предоставляет...", "Реализовано в модуле..."
- Технические термины: "форсинг-движок", "трёхзначная логика", "иммутабельность"
- Описательный стиль: "Модуль содержит...", "Функция выполняет..."
- Императивные конструкции в инструкциях: "Установите зависимости", "Запустите тесты"

**Запрещено:**
- Эмоциональные выражения: "Отлично!", "Круто!", "Всё работает!"
- Неформальные обороты: "Приятного изучения!", "Made with ❤️"
- Избыточные восклицательные знаки
- Разговорные конструкции: "Давайте посмотрим...", "Попробуем сделать..."

### Маркеры и символы

**Использование галочек и маркеров:**
- ✅ Используй в таблицах для обозначения статуса: "✅ Реализовано" vs "❌ Не реализовано"
- ❌ НЕ используй в заголовках разделов: "### ✅ Реализовано" → "### Реализовано"
- ❌ НЕ используй в обычных списках: "- ✅ Базовые тесты" → "- Базовые тесты"

**Эмодзи:**
- Разрешены навигационные эмодзи: 🏠 (главная), 📖 (содержание), ← → (навигация)
- Разрешены в заголовках для визуальной навигации: "## 📖 Содержание"
- Запрещены эмоциональные эмодзи: ❤️ 🎉 ✨

### Статистика и метрики

**Запрещено включать:**
- Количество строк: "942 строки", "2,119 строк"
- Размеры файлов: "30 KB", "~69 KB"
- Процентные показатели: "+344%", "увеличение в 4.4 раза"
- Временные оценки: "1-2 часа", "30-60 мин", "Время чтения: 5 мин"
- Подсчёты: "Всего слов: ~6,371", "Навигационных ссылок: ~100+"

**Исключение:**
- Технические метрики в описании алгоритмов: "O(n)", "время выполнения"

### Структура документов

#### Обязательные элементы

1. **Навигация (в начале и в конце):**
   ```markdown
   > **Навигация:** [🏠 Главная](README.md) | [← Предыдущее] | **Вы здесь** | [Следующее →]
   ```

2. **Оглавление (для документов > 100 строк):**
   ```markdown
   ## 📖 Содержание
   - [Раздел 1](#раздел-1)
   - [Раздел 2](#раздел-2)
   ```

3. **Связи с другими документами:**
   ```markdown
   > **См. также:**
   > - [USAGE.md](USAGE.md)
   > - [FORCING_IMPLEMENTATION.md](FORCING_IMPLEMENTATION.md)
   ```

#### Типы документов

**README.md:**
- Главная точка входа
- Краткий обзор проекта
- Пути обучения (Новичок/Разработчик/Исследователь)
- Ссылки на всю документацию

**USAGE.md:**
- Практическое руководство
- Примеры использования с кодом
- Лучшие практики
- Типичные сценарии

**{КОМПОНЕНТ}_IMPLEMENTATION.md:**
- Техническое описание реализации
- Архитектура и алгоритмы
- Таблицы истинности, псевдокод
- Граничные случаи

**scenarios/README.md:**
- Руководство по созданию сценариев
- Структура сценария
- Примеры и шаблоны

**DOCUMENTATION_SUMMARY.md:**
- Обзор всей документации
- Покрытие тем
- Руководство по использованию документации

### Примеры кода в документации

**Правила:**
- Всегда указывай язык: ` ```python `
- Используй полные примеры (с импортами)
- Не добавляй нумерацию строк в код
- Добавляй комментарии только для сложной логики
- Показывай ожидаемый вывод в отдельном блоке

**Пример оформления:**

```markdown
### Пример: Проверка форсинга

```python
from ctmsn.forcing.engine import ForcingEngine

engine = ForcingEngine(net)
result = engine.forces(ctx, phi, conditions)
```

**Ожидаемый вывод:**
```
forces: true
force result: true
```
```

### Термины и определения

**Используй единообразную терминологию:**
- "форсинг-движок" (не "forcing engine" в русском тексте)
- "трёхзначная логика" (не "трехзначная")
- "семантическая сеть" (не "сематическая")
- "контекст" (не "контекст выполнения")
- "форсирование" (не "принуждение")

### Обновление документации

**При изменении кода:**
1. Обнови соответствующие разделы в USAGE.md
2. Проверь примеры кода на актуальность
3. Обнови технические описания в IMPLEMENTATION.md
4. Проверь корректность ссылок

**При добавлении функционала:**
1. Добавь описание в README.md (раздел "Возможности")
2. Создай примеры использования в USAGE.md
3. Опиши реализацию в соответствующем IMPLEMENTATION.md
4. Обнови навигацию между документами

## Запрещенные практики

### В коде:
- Не используй mutable значения по умолчанию
- Не модифицируй объекты напрямую (используй with_* методы)
- Не приводи TriBool к bool
- Не используй глобальное состояние
- Не добавляй внешние зависимости без необходимости
- Не пиши комментарии к очевидному коду

### В документации:
- Не добавляй статистику (строки, размеры, проценты)
- Не используй временные оценки ("1-2 часа", "30 мин")
- Не используй неформальные выражения ("Круто!", "Отлично!")
- Не ставь галочки ✅ в заголовки и обычные списки
- Не создавай отчётные документы (REPORT.md) — они не часть документации
- Не используй эмоциональные эмодзи (❤️ 🎉 ✨) кроме навигационных

## Команды для разработки

```bash
source venv/bin/activate

python src/ctnss/examples/hello_forcing.py

python src/ctnss/examples/example_usage.py

python tests/test_smoke_imports.py
```

## Будущие направления

- Реализация стратегий поиска в forcing/strategy.py
- Механизм объяснений (explainability)
- Кванторы в логическом слое
- Полноценная сериализация
- Комплексные тесты
