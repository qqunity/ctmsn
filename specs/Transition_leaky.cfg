\* Дефектная конфигурация: TLC ДОЛЖЕН найти нарушение Consistency
\* (объект теряется на переходе s0->s1). Это формально подтверждает,
\* что денотационный инвариант ловит дефект, который baseline пропускает.
SPECIFICATION Spec
CONSTANTS
    N = 4
    Faulty = TRUE
INVARIANT TypeOK
INVARIANT Consistency
