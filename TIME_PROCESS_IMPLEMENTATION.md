# Реализация сценария "Процесс во времени"

> **Навигация:** [🏠 Главная](README.md) | [← Scenarios](src/ctmsn/scenarios/README.md) | [Scenario README](src/ctmsn/scenarios/time_process/README.md)

## 📖 Содержание

- [Обзор](#обзор)
- [Теоретические основы](#теоретические-основы)
- [Архитектура](#архитектура)
- [Механизм вывода](#механизм-вывода)
- [Режимы работы](#режимы-работы)
- [Примеры](#примеры)
- [Расширения](#расширения)

---

## Обзор

Сценарий "Процесс во времени" демонстрирует моделирование темпоральных процессов через композицию морфизмов в категориальной семантике. Реализация основана на диаграмме 4.13 из книги.

### Ключевые особенности

- Трёхуровневая формализация (диаграмма → равенства → вычисление)
- Механизм вывода контекстных стрелок из базовых морфизмов
- Композиционные равенства с полной трассировкой
- Два режима: sun (солнечный процесс) и prereq (prerequisite/effect)

---

## Теоретические основы

### Уровень L1: Диаграмма как семантическая сеть

Вершины и стрелки представлены как факты `edge(label, from, to)`:

```
B′ --before--> A --after--> B
       |        |        |
       v        v        v
      T−  <--- T ---> T+
```

### Уровень L2: Канонические равенства композиций

Проверяются высказывания вида:
- `below ∘ sun_before = sunset`
- `above ∘ sun_after = sunrise`
- `g⁻ ∘ h_before = h⁻`
- `g⁺ ∘ h_after = h⁺`

### Уровень L3: Процесс как вычисление

Из базовых стрелок `before`/`after` и `sun` выводятся "контекстные" стрелки:
- `sun_before: B′ → T` (выводится из `before: B′ → A` и `sun: A → T`)
- `sun_after: B → T` (выводится из `after: B → A` и `sun: A → T`)

После этого проверяются композиционные равенства по 2-шаговым путям.

---

## Архитектура

### Структура модулей

```
src/ctmsn/scenarios/time_process/
├── __init__.py          # Инициализация пакета
├── model.py             # Базовая модель (только edges)
├── derive.py            # Механизм вывода (D1 + D2)
├── constraints.py       # Условия целостности
├── goal.py              # Целевые формулы
├── explain.py           # Форматирование объяснений
├── params.py            # Параметры (для будущего расширения)
├── runner.py            # Запуск сценария
└── README.md            # Описание сценария
```

### Предикаты

- `edge(label, from, to)` — базовые стрелки диаграммы
- `derived_edge(label, from, to)` — выведенные стрелки
- `comp(left, right, result)` — композиционные равенства
- `comp_expl(left, right, result, mid)` — трассировка композиций

---

## Механизм вывода

### Шаг D1: Вывод контекстных стрелок

**Правило для режима sun:**
```
edge("before", B′, A) ∧ edge("sun", A, T)
⟹ derived_edge("sun_before", B′, T)

edge("after", B, A) ∧ edge("sun", A, T)
⟹ derived_edge("sun_after", B, T)
```

**Правило для режима prereq:**
```
edge("prerequisite", B′, A) ∧ edge("h", A, T)
⟹ derived_edge("h_before", B′, T)

edge("effect", B, A) ∧ edge("h", A, T)
⟹ derived_edge("h_after", B, T)
```

### Шаг D2: Вывод композиционных равенств

**Правило:**
```
edge/derived_edge(left, X, mid) ∧
edge/derived_edge(right, mid, Z) ∧
edge/derived_edge(result, X, Z)
⟹ comp(left, right, result) ∧ comp_expl(left, right, result, mid)
```

**Интерпретация:**
Если существует путь X -left→ mid -right→ Z и прямая стрелка X -result→ Z, то `result = right ∘ left`.

### Алгоритм вывода

```python
def apply(net: SemanticNetwork, mode: str):
    # D1: Вывод контекстных стрелок
    added_edges = derive_context_edges(net, mode)
    
    # D2: Вывод композиций
    comps = derive_comp(net)
    for c in comps:
        net.assert_fact("comp", (c.left, c.right, c.result))
        net.assert_fact("comp_expl", (c.left, c.right, c.result, c.mid))
```

---

## Режимы работы

### Режим "sun" (солнечный процесс)

**Узлы:**
- `B′` (прошлое), `A` ({now1}), `B` (будущее)
- `T`, `T−`, `T+` (временные точки)
- `horizon1` (горизонт)

**Базовые стрелки:**
```
before: B′ → A
after:  B  → A
sun:    A  → T
below:  T  → T−
above:  T  → T+
sunset: B′ → T−  (результат композиции)
sunrise: B → T+  (результат композиции)
```

**Выводимые стрелки:**
```
sun_before:  B′ → T  (из before + sun)
sun_after:   B  → T  (из after + sun)
sun1_before: B′ → horizon1
sun1_after:  B  → horizon1
```

**Проверяемые композиции:**
```
below ∘ sun_before = sunset
above ∘ sun_after = sunrise
below_h ∘ sun1_before = sunset_h
above_h ∘ sun1_after = sunrise_h
```

### Режим "prereq" (prerequisite/effect)

**Узлы:**
- `B′` (прошлое), `A` (текущее), `B` (будущее)
- `T`, `T−`, `T+`

**Базовые стрелки:**
```
prerequisite: B′ → A
effect:       B  → A
h:            A  → T
g_minus:      T  → T−
g_plus:       T  → T+
h_minus:      B′ → T−  (результат)
h_plus:       B  → T+  (результат)
```

**Выводимые стрелки:**
```
h_before: B′ → T  (из prerequisite + h)
h_after:  B  → T  (из effect + h)
```

**Проверяемые композиции:**
```
g_minus ∘ h_before = h_minus
g_plus ∘ h_after = h_plus
```

---

## Примеры

### Пример 1: Режим sun

```python
from ctmsn.scenarios.time_process.runner import run

out = run(mode="sun")
print("Derivation stats:", out["derivation"])
# {'derived_edges_added': 4, 'comp_added': 12, 'comp_expl_added': 12}

print("forces(phi):", out["forces"].value)
# true

for line in out["explain"]:
    print("  -", line)
# - sunset = below ∘ sun_before (через узел T)
# - sunrise = above ∘ sun_after (через узел T)
```

### Пример 2: Режим prereq

```python
out = run(mode="prereq")
print("Check ok:", out["check"].ok)
# True

for line in out["explain"]:
    print("  -", line)
# - h_minus = g_minus ∘ h_before (через узел T)
# - h_plus = g_plus ∘ h_after (через узел T)
```

### Пример 3: Прямое использование derive

```python
from ctmsn.scenarios.time_process.model import build_network
from ctmsn.scenarios.time_process import derive

net = build_network(mode="sun")

# Вывод контекстных стрелок
added = derive.derive_context_edges(net, mode="sun")
print(f"Добавлено контекстных стрелок: {added}")

# Вывод композиций
comps = derive.derive_comp(net)
for c in comps:
    print(f"{c.result} = {c.right} ∘ {c.left} (через {c.mid})")
```

---

## Расширения

### Подготовка к форсингу

Сценарий подготовлен к будущему форсингу:

1. **Неполная модель:** Можно собрать сеть без `sunset` или `sunrise`
2. **Поиск результата:** Форсинг может предложить, какую стрелку добавить
3. **Параметризованные запросы:** Через `params.py` можно задавать переменные

### Пример неполной модели

```python
def build_incomplete_network():
    net = SemanticNetwork()
    # ... добавляем узлы и базовые стрелки ...
    # НЕ добавляем sunset и sunrise
    
    # Вопрос: какая стрелка должна быть результатом below ∘ sun_before?
    return net
```

### Будущие возможности

- **Поиск недостающих стрелок:** Форсинг может определить, что нужно добавить `sunset`
- **Проверка корректности:** Валидация композиционных равенств
- **Объяснения противоречий:** Если композиция не выполняется

---

## Тестирование

### Структура тестов

```
tests/scenarios/
├── __init__.py
├── test_time_process_builds.py      # Построение сети
├── test_time_process_derivation.py  # Вывод стрелок и композиций
└── test_time_process_goals.py       # Проверка целей
```

### Запуск тестов

```bash
# Тест построения сети
python3 tests/scenarios/test_time_process_builds.py

# Тест механизма вывода
python3 tests/scenarios/test_time_process_derivation.py

# Тест целей и форсинга
python3 tests/scenarios/test_time_process_goals.py
```

### Ключевые проверки

- Корректность построения сети для обоих режимов
- Вывод контекстных стрелок (D1)
- Вывод композиционных равенств (D2)
- Проверка условий и целей
- Корректность объяснений

---

## См. также

- **[README.md](README.md)** — общий обзор проекта
- **[USAGE.md](USAGE.md)** — практическое руководство
- **[FORCING_IMPLEMENTATION.md](FORCING_IMPLEMENTATION.md)** — архитектура форсинга
- **[scenarios/time_process/README.md](src/ctmsn/scenarios/time_process/README.md)** — описание сценария
- **[scenarios/README.md](src/ctmsn/scenarios/README.md)** — руководство по созданию сценариев

---

> **Навигация:** [🏠 Главная](README.md) | [Scenarios](src/ctmsn/scenarios/README.md) | [Fast Smith](FAST_SMITH_IMPLEMENTATION.md)
