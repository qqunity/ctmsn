# Лабораторная работа 2. Анализ готовых семантических сетей

**Предметная область:** Четыре референсных сценария CTMSN

**Система:** CTMSN (Composable Typed Modules for Semantic Networks)

## Содержание

- [Цель работы](#1-цель-работы)
- [Теоретические сведения](#2-теоретические-сведения)
- [Требования к оборудованию и ПО](#3-требования-к-оборудованию-и-по)
- [Порядок выполнения](#4-порядок-выполнения)
- [Задание для самостоятельной работы](#5-задание-для-самостоятельной-работы)
- [Требования к отчёту](#6-требования-к-отчёту)
- [Контрольные вопросы](#7-контрольные-вопросы)

---

## 1 Цель работы

Освоить навыки анализа готовых семантических сетей в системе CTMSN: загрузка сценариев, навигация по графу, изучение уравнений композиции, сравнение режимов работы, экспорт результатов и составление сводной статистики.

## 2 Теоретические сведения

### 2.1 Композиция предикатов

Композиция — операция получения нового отношения из двух существующих путём «склеивания» по промежуточному узлу. В системе CTMSN различают два вида композиции:

- **Бинарная композиция (comp2)** — два морфизма соединяются через общий промежуточный узел. Записывается как `g ∘ f = h`, где `f: A → B`, `g: B → C`, `h: A → C`. В графе промежуточный узел `B` связывает рёбра `f` и `g`, результирующее ребро `h` является производным.
- **Цепочечная композиция (compN)** — обобщение бинарной: несколько морфизмов соединяются последовательно. Записывается как `gₙ ∘ … ∘ g₁ ∘ f = h`. Длинные цепочки позволяют выражать многошаговые процессы.

### 2.2 Производные рёбра

При загрузке сценария с включённым флагом «Derive» система автоматически вычисляет производные рёбра. Производное ребро — это ребро, существование которого следует из уравнений композиции. На графе производные рёбра отображаются пунктирной линией и отличаются по цвету от исходных.

### 2.3 Панель уравнений

Панель «Уравнения» отображает список всех уравнений композиции текущего сценария. При выборе уравнения на графе подсвечиваются участвующие рёбра и узлы, что позволяет визуально проследить путь композиции.

### 2.4 Панель статистики сети

Панель «Статистика сети» отображает четыре числовые характеристики загруженной семантической сети:

| Параметр   | Описание                                                |
|------------|---------------------------------------------------------|
| Концептов  | Количество узлов (концептов) в сети                     |
| Предикатов | Количество уникальных предикатов (типов отношений)      |
| Фактов     | Количество рёбер (включая производные)                  |
| Уравнений  | Количество уравнений композиции (comp2 + compN)         |

### 2.5 Описание сценариев

#### Сценарий `fast_smith` (задача Смита, п. 4.12)

Моделирует классическую задачу динамики идентичности. Индивид A (Смит) вступает в брак с S, меняя фамилию на J (Джонс). Сеть описывает контексты «до» и «после» перехода, морфизмы между состояниями и композиции, связывающие идентичности.

**Основные элементы:**

| Элемент     | Примеры                                                    |
|-------------|------------------------------------------------------------|
| Концепты    | A (Смит), B, T, T+, T-, Cf-, Cf+, S, J, C                |
| Предикаты   | edge, comp, in, has_name, married, acts_like               |
| Морфизмы    | f, s, sf, h, g, not-g, r, j, jf                           |
| Композиции  | g ∘ h = j, not-g ∘ h = s, r ∘ sf = jf                     |

#### Сценарий `time_process` (процесс во времени, п. 4.13)

Моделирует процесс, разворачивающийся во времени через композицию морфизмов. Имеет два режима:

- **Режим `sun`** — моделирует процесс восхода и заката солнца. Узлы B' (прошлое), A (настоящее), B (будущее); предикаты описывают положение солнца относительно горизонта.
- **Режим `prereq`** — моделирует схему «предусловие → эффект» с абстрактными морфизмами h, g_minus, g_plus.

Сравнение режимов позволяет увидеть, как одна и та же структура (процесс во времени) конкретизируется в разных предметных областях.

| Режим   | Специфические узлы | Специфические морфизмы                                |
|---------|--------------------|-------------------------------------------------------|
| sun     | horizon            | before, after, sun, below, above, sunset, sunrise     |
| prereq  | —                  | prerequisite, effect, h, g_minus, g_plus, h_minus, h_plus |

#### Сценарий `fishing` (рыбная ловля, п. 4.14)

Моделирует процесс рыбной ловли с разделением рыб на два класса: F+ (пойманные) и F- (непойманные). Сеть описывает поведение червя (приманки), крючка и процесс ловли через многошаговую композицию.

**Основные элементы:**

| Элемент     | Примеры                                                     |
|-------------|-------------------------------------------------------------|
| Концепты    | A, B, F, F+, F-, W, W+, Cf-, Cf+                           |
| Морфизмы    | h, s, j, g_minus, g_plus, catch, sf, eat, fake+, hook+     |
| Композиции  | h ∘ g_minus = s, h ∘ g_plus = j, s ∘ catch = j             |
| Длинная     | hook+ ∘ fake+ ∘ eat ∘ sf = catch_sf                        |

#### Сценарий `spawn` (нерест, п. 4.15)

Моделирует процесс нереста рыб со сменой ролей (метание икры / поедание икры) на разных стадиях развития A → B → C. Демонстрирует операторы поведения `push` (прекращение поедания) и `rethink` (возврат к поеданию).

**Основные элементы:**

| Элемент     | Примеры                                                          |
|-------------|------------------------------------------------------------------|
| Концепты    | A, B, C (стадии), Fish, Fish_minus, Fish_plus, Cf_minus, Cf_plus|
| Морфизмы    | f, e, fish, not_eat, eat, spawner, milter, push, rethink         |
| Композиции  | not_eat ∘ fish = spawner, eat ∘ fish = milter                    |
| Поведение   | push ∘ milter_f = spawner_f, rethink ∘ spawner_f = milter_f      |

## 3 Требования к оборудованию и ПО

- Компьютер с доступом в интернет
- Веб-браузер (Chrome, Firefox, Safari, Edge)
- Доступ к сайту https://ctmsn.ru

## 4 Порядок выполнения

### 4.1 Вход в систему

1. Откройте в браузере сайт https://ctmsn.ru.
2. Войдите в систему, используя учётную запись, созданную в Лабораторной работе 1.
3. При необходимости зарегистрируйтесь (формат логина: `Фамилия_ИО_группа`).

### 4.2 Загрузка первого сценария (fast_smith)

1. На странице «Мои пространства» создайте новое рабочее пространство (например, «ЛР2 — Сценарии»).
2. Перейдите в рабочую область.
3. В верхней панели в выпадающем списке сценариев выберите `fast_smith`.
4. Убедитесь, что флаг **«Derive»** включён (производные рёбра будут вычислены автоматически).
5. Нажмите кнопку **«Загрузить»**.
6. Дождитесь отображения графа семантической сети.

### 4.3 Изучение графа

1. **Навигация** — перемещайте граф мышью; масштабируйте колесом прокрутки.
2. **Выделение узла** — щёлкните по узлу графа. В правой панели **«Details»** отобразятся свойства выбранного концепта: идентификатор, метка, теги.
3. **Выделение ребра** — щёлкните по ребру. В панели **«Details»** отобразятся: метка ребра, предикат, исходный и целевой узлы, тип (исходное/производное).
4. **Легенда** — в левом нижнем углу расположена легенда с цветовой кодировкой рёбер.
5. **Раскладка** — используйте селектор раскладки для переключения алгоритма расположения узлов.

### 4.4 Панель статистики сети

1. В правой панели первым элементом отображается панель **«Статистика сети»**.
2. Зафиксируйте четыре значения: количество концептов, предикатов, фактов (рёбер) и уравнений.
3. Запишите эти значения в сводную таблицу (см. раздел 5).

### 4.5 Панель уравнений

1. Прокрутите правую панель до раздела **«Уравнения»**.
2. В списке отображаются все уравнения композиции текущего сценария.
3. Щёлкните по уравнению — на графе подсветятся участвующие рёбра и узлы.
4. Для уравнения типа comp2 (бинарная композиция) обратите внимание на три ребра: левое, правое и результирующее, а также на промежуточный узел.
5. Для уравнения типа compN (цепочечная композиция) проследите всю цепочку морфизмов на графе.
6. Выберите наиболее интересное уравнение и опишите его семантику в отчёте.

### 4.6 Экспорт JSON

1. Нажмите кнопку **«📥 JSON»** на панели инструментов.
2. Система сформирует JSON-файл с полным состоянием рабочего пространства и предложит сохранить его.
3. Сохраните файл для приложения к отчёту.

### 4.7 Экспорт скриншота графа

1. Нажмите кнопку **«📷 PNG»** на панели инструментов.
2. Сохраните полученное изображение.

### 4.8 Загрузка остальных сценариев

Повторите шаги 4.2–4.7 для каждого из оставшихся сценариев:

1. **`time_process`** (режим `sun`) — в выпадающем списке сценариев выберите `time_process`, в списке режимов выберите `sun`. Нажмите **«Загрузить»**.
2. **`time_process`** (режим `prereq`) — не покидая рабочее пространство, переключите режим на `prereq` и нажмите **«Загрузить»** повторно.
3. **`fishing`** — выберите сценарий `fishing`, нажмите **«Загрузить»**.
4. **`spawn`** — выберите сценарий `spawn`, нажмите **«Загрузить»**.

Для каждого сценария:
- Зафиксируйте статистику сети.
- Изучите уравнения.
- Экспортируйте JSON и скриншот.

### 4.9 Сравнение режимов time_process

1. Загрузите сценарий `time_process` в режиме `sun`.
2. Запишите статистику: количество концептов, предикатов, фактов, уравнений.
3. Сделайте скриншот графа.
4. Переключите режим на `prereq` и нажмите **«Загрузить»**.
5. Запишите статистику для режима `prereq`.
6. Сделайте скриншот графа.
7. Сравните два режима:
   - Какие узлы присутствуют в одном режиме и отсутствуют в другом?
   - Какие морфизмы специфичны для каждого режима?
   - Как различаются уравнения композиции?

### 4.10 Использование справочной панели

1. Нажмите кнопку **«?»** (справка) на панели инструментов.
2. На вкладке **«Задание»** отображается описание текущей лабораторной работы, привязанное к загруженному сценарию.
3. Вкладки **«Логика»** и **«Глоссарий»** содержат справочные материалы по трёхзначной логике и терминологии.

## 5 Задание для самостоятельной работы

### 5.1 Сводная таблица

Загрузите все четыре сценария и заполните сводную таблицу:

| Сценарий             | Концепты | Предикаты | Факты | Уравнения |
|----------------------|----------|-----------|-------|-----------|
| fast_smith           |          |           |       |           |
| time_process (sun)   |          |           |       |           |
| time_process (prereq)|          |           |       |           |
| fishing              |          |           |       |           |
| spawn                |          |           |       |           |

### 5.2 Описание предметных областей

Для каждого сценария напишите краткое описание предметной области (2–3 предложения):
- Какие сущности моделируются?
- Какие отношения между ними выделены?
- Какой процесс или явление описывается?

### 5.3 Анализ уравнений композиции

Для каждого сценария выберите одно уравнение композиции и:
- Запишите его в формальном виде (например, `g ∘ f = h`).
- Назовите участвующие морфизмы и промежуточный узел.
- Объясните семантику: что означает данная композиция в терминах предметной области.

### 5.4 Сравнение режимов

Для сценария `time_process` выполните сравнительный анализ режимов `sun` и `prereq`:
- Укажите различия в количестве концептов, предикатов, фактов и уравнений.
- Перечислите узлы и морфизмы, специфичные для каждого режима.
- Объясните, как одна абстрактная структура (процесс во времени) конкретизируется в двух разных предметных областях.

## 6 Требования к отчёту

Отчёт оформляется в соответствии с ГОСТ 7.32-2017 и должен содержать следующие разделы:

1. **Титульный лист** — наименование учебного заведения, кафедры, дисциплины; название работы; ФИО студента и преподавателя; год.
2. **Цель работы** — формулировка цели в соответствии с разделом 1 настоящей инструкции.
3. **Теоретическая часть** — краткое изложение понятий: композиция предикатов (бинарная и цепочечная), производные рёбра, панель уравнений, статистика сети.
4. **Ход работы** — пошаговое описание действий в системе CTMSN с приложением скриншотов:
   - Скриншот графа для каждого из четырёх сценариев.
   - Скриншот панели статистики для каждого сценария.
   - Скриншот подсвеченного уравнения (для одного из сценариев).
   - Скриншоты двух режимов `time_process` для сравнения.
5. **Результаты:**
   - Заполненная сводная таблица (раздел 5.1).
   - Описание предметных областей (раздел 5.2).
   - Анализ уравнений композиции (раздел 5.3).
   - Сравнительный анализ режимов `time_process` (раздел 5.4).
6. **Выводы** — заключение о проделанной работе, соответствие результатов поставленной цели.
7. **Приложения** — экспортированные JSON-файлы для всех сценариев.

## 7 Контрольные вопросы

1. Что такое бинарная композиция предикатов? Запишите в общем виде.
2. Чем бинарная композиция (comp2) отличается от цепочечной (compN)?
3. Что такое производное ребро? Как оно отображается на графе?
4. Какую роль играет промежуточный узел в уравнении бинарной композиции?
5. Опишите предметную область сценария `fast_smith`. Какие контексты моделируются?
6. В чём различие режимов `sun` и `prereq` сценария `time_process`?
7. Какую многошаговую композицию описывает сценарий `fishing`? Запишите формально.
8. Что моделируют операторы `push` и `rethink` в сценарии `spawn`?
9. Для чего используется экспорт в формате JSON?
10. Какие числовые характеристики отображает панель статистики сети?
